
Core JavaScript (Basics)
1. variables 
2. data Types
3. Operators
4. type conversion
5. conditional statements
6. loops

Functions Basics - Part 1
7. functions

Strings and Arrays 
8. strings and string functions
9. arrays and array functions
10. string <-> array 

Objects and Advanced Syntax
11. Objects (Creation, Accessing, Deleting, Updating, Objects Keys and Methods, Nested Objects)
12. Destructuring (Object and Array), Nested Destructuring, Array of Objects
13. Pass by Value and Pass by Reference
14. Rest and Spread operators

Numbers and Utilities
15. Special Numbers and Math functions

Functions - Part 2
16. Scope of a Variable, Hoisting and Closures 
17. Types of Functions (Fn Declaration, Fn Expression, Arrow Functions) and Anonymous Functions.

Callbacks and Array Iteration 
18. Callbacks and its Different forms
19. forEach and Array Iteration Callback Functions (find(), filter(), sort(), map(), reduce())

DOM and Broswer 
20. DOM and CRUD operations in DOM 
21. BOM (Browser Object Model)

Events
22. Events and Event Handling 
23. Form Events
24. keyBoard Events
25. Mouse Events 
27. Event Bubbling

Error and Data Handling
28. Error Handling
29. JSON, JSON.parse(), JSON.stringify()

Networking
30. HTTP Request and Repsonse , HTTP Method (GET, POST, PUT, DELETE), Handling HTTP Response, HTTP Status Codes
31. Web APIs 

Async Js
32. Asynchronous Programming (Introduction)
       |-> Promises 
       |-> Async / Await 
33. Promises , States of a Promise, Handling Promise
34. fetch API , POST request using fetch 
35. Async - Await 

Storage
36. localStorgae (setItem, getItem, removeItem, clear)

=================================================================================================

Good gp, now 20th Folder

20th Folder : DOM

index.html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DOM</title>

    <style>
        li{
            list-style: none;
        }
        div{
            display: flex;
            justify-content: space-around;
            align-items: center;
            width: 300px;
        }
    </style>

</head>
<body>

    <h1>DOM (Document Object Model)</h1>
    <h4>Check the script.js & notes.txt file for details</h4>

    <h2>To-Do List</h2>
    <input type="text" id="inputTask" placeholder="Enter Details">
    <button onclick="addItem()">Add Item</button>

    <ul id="taskList"></ul>

    <script src="./script.js"></script>

</body>
</html>


script.js
let taskList = document.getElementById("taskList");

function addItem(){
    let addItem = document.getElementById("inputTask").value.trim();

    if(addItem === "") return;

    let newItem = document.createElement('li');
    
    let itemDiv = document.createElement('div');    
    // itemDiv.id = "item" + (taskList.childElementCount) + 1;

    let itemHeading = document.createElement('h3');
    let taskText = document.createTextNode(addItem);
    itemHeading.appendChild(taskText);

    let itemUpdateBtn = document.createElement('button');
    let itemUpdateText = document.createTextNode('Done');
    itemUpdateBtn.appendChild(itemUpdateText);
    itemUpdateBtn.onclick = function(){
        updateItem(this);
    };
    // itemUpdateBtn.id = "item" + (taskList.childElementCount) + 1;

    let itemDeleteBtn = document.createElement('button');
    let itemDeleteText = document.createTextNode('Delete');
    itemDeleteBtn.appendChild(itemDeleteText);
    itemDeleteBtn.onclick = function(){
        deleteItem(this);
    };
    // itemDeleteBtn.id = "item" + (taskList.childElementCount) + 1;

    itemDiv.appendChild(itemHeading);
    itemDiv.appendChild(itemUpdateBtn);
    itemDiv.appendChild(itemDeleteBtn);

    newItem.appendChild(itemDiv);

    taskList.appendChild(newItem);

    document.getElementById("inputTask").value = "";
}

function updateItem(button){
    let div = button.parentElement;
    let heading = div.querySelector("h3");
    heading.style.textDecoration = "line-through";
}

function deleteItem(button){
    let li = button.parentElement.parentElement;
    li.remove();
}

notes.txt 
DOM (Document Object Model):

Introduction:

=> Js can't understand HTML. Js understand datatypes. It will refer it as document.
    HTML in terms of Js is a document.
=> When a page is loaded, the browser creates a DOM of page.
=> Tree representation of the HTML document.
=> DOM tree can be modified with the help of Js.
=> It is an object oriented representation.

                                Document
                                   |
                                Root <html>
                    _______________|_______________
                    |                             |
                  <head>                        <body>
                    |                     ________|________          |
                  <title>                 |               |
                    |                    <h1>            <a> -> href
                  Text "My Title"         |               |
                                        My header        text
                    
=> Using this we can access the HTML elements, attributes, CSS stylings and Events.

==============================================================================

CRUD Operations with HTML Elements:

Create HTML Elements:
1) createElement('tag')     - is used to create an HTML Element.
2) createTextNode('text')   - is used to place the string in the created element. 
3) parent.insertBefore(created Element, sibling Element) - to insert element before an element. called on parent element
4) firstElementChild        - returns the first element of list.
5) cloneNode(true)          - copies element.

Add, Remove & Toggle classes from HTML elements:

classList: It holds all the classes of an HTML element.

add()    :  used to add class (adds class name for an existing HTML element)
remove() :  removes class name from an existing HTML element
toggle() :  to toggle a class name (if it exits then removes or else adds a class name to HTML element)

-------------------------------------------------

Read Operation
Select HTML Elements:
1) getElementById()
2) getElementByTagName()
3) getElementByClassName()

Query Selectors: css selectors to select HTML elements
4) querySelector()     - selects 1st element of class 
5) querySelectorAll()  - selects all elements of class 

-------------------------------------------------

Update HTML Elements:
1) element.innerHTML       - new HTML element
2) element.attribute       - new value 
    element.setAttribute('attr','value')
    element.getAttribute('attr')
3) element.style.property  - new style 
4) element.innerText       - only for updating text and considers only visible text.
5) element.textContent     - only for updating text but considers invisible text by CSS.

6) replaceChild('new created element', 'selected element to be replaced') 
                           - used for replacing elements and must be called on parent element.
7) appendChild()           - appends a new HTML element to an existing one as a Child Element
8) element.append()        - can append text and elements.
9) element.prepend()       - inserts at beginning.
10) element.replaceWith()  - modern alternative to replaceChild().

-------------------------------------------------

Delete HTML Elements:
1) parent.removeChild('selected Element) - to delete an HTML element. called on parent element
2) element.remove()                      - removes element directly without referencing parent.

-------------------------------------------------

Additional:

1) childElementCount  - returns the no. of child elements
==============================================================================

dom manipulation

questions

1. difference: getElementById vs querySelector
2. innerHTML vs textContent
3. reflow vs repaint (basic idea)

problems

1. build todo list (must do)
2. create element dynamically and append
3. highlight searched text
4. remove all child nodes
5. live character counter

The above is the 20th topic, analyze the html, js files and notes.txt. For this folder just do changes in HTML if you feel required. 
But generate a good README file with the information provided in notes.txt and additional information which you feel would be better 
for this with proper codes and nice examples for better understanding.  Also maintain the diagram which I mentioned in notes.txt.

(1)
The above is the 20th topic, analyze the html, js files and notes.txt. For this folder just do changes in HTML if you feel required. 
But generate a good README file with the information provided in notes.txt and additional information which you feel would be better 
for this with proper codes and nice examples for better understanding. 

(2)
The above is the 20th topic, analyze the html, js files and notes.txt and do the same thing as you did for previous folders. 
Also as you can see we used HTML file so you can do changes in HTML also, if required and help me in creating html, js and README files 
with required information and codes with nice examples for better understanding. 

(3)
The above is the 19th topic, analyze the html, js files and notes.txt and do the same thing as you did for previous folders. 
Also as you can see we used HTML file so you can do changes in HTML also, if required and help me in creating html, js and README files 
with required information and codes with nice examples for better understanding. And try to maintain the points I have written in my notes.txt
including extra points for better understanding. You can any additional data that is helpful for this but the data mentioned in my notes.txt
should also be mentioned in new README.md file

(4)
The above is 16th topic, I haven't created any js file because I am not getting any idea for this. 
Also as you can see I didn't use the HTML file very much so suggest few changes we can do for HTML.
Help me in creating html, js and README files with required information and codes with nice examples for better understanding.

(5)
The above is the 7th topic, I have not written any notes for this topic, so generate a new README file, analyze the html and js files and do the same thing as you did for previous folders. 
Also as you can see we used HTML file so you can do changes in HTML also, if required.