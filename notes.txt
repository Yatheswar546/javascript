
Core JavaScript (Basics)
1. variables 
2. data Types
3. Operators
4. type conversion
5. conditional statements
6. loops

Functions Basics - Part 1
7. functions

Strings and Arrays 
8. strings and string functions
9. arrays and array functions
10. string <-> array 

Objects and Advanced Syntax
11. Objects (Creation, Accessing, Deleting, Updating, Objects Keys and Methods, Nested Objects)
12. Destructuring (Object and Array), Nested Destructuring, Array of Objects
13. Pass by Value and Pass by Reference
14. Rest and Spread operators

Numbers and Utilities
15. Special Numbers and Math functions

Functions - Part 2
16. Scope of a Variable, Hoisting and Closures 
17. Types of Functions (Fn Declaration, Fn Expression, Arrow Functions) and Anonymous Functions.

Callbacks and Array Iteration 
18. Callbacks and its Different forms
19. forEach and Array Iteration Callback Functions (find(), filter(), sort(), map(), reduce())

DOM and Broswer 
20. DOM and CRUD operations in DOM 
21. BOM (Browser Object Model)

Events
22. Events and Event Handling 
23. Form Events
24. keyBoard Events
25. Mouse Events 
27. Event Bubbling

Error and Data Handling
28. Error Handling
29. JSON, JSON.parse(), JSON.stringify()

Networking
30. HTTP Request and Repsonse , HTTP Method (GET, POST, PUT, DELETE), Handling HTTP Response, HTTP Status Codes
31. Web APIs 

Async Js
32. Asynchronous Programming (Introduction)
       |-> Promises 
       |-> Async / Await 
33. Promises , States of a Promise, Handling Promise
34. fetch API , POST request using fetch 
35. Async - Await 

Storage
36. localStorgae (setItem, getItem, removeItem, clear)

=================================================================================================

Good gp, now 16th Folder

16th Folder : Scope, Hoisting and Closures

index.html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Scope, Hoisting and Closures</title>
</head>
<body>

    <h1>Scope, Hoisting and Closures</h1>
    <h4>Check the notes.txt file for details</h4>

</body>
</html>

notes.txt 
Scope of a Variable:

        Types 
    ______|______
    |           |
  Global       Local 
                |-> Function (var)
                |-> Block (let, const)

Function Scope: 
=> variables can't be accessed outside the function using "var/let/const".
=> Used especially in "functions".

Eg: function func(){
        var b = "Hello";
        console.log(b);  // Hello
    }
    func();
    console.log(b)  // error

Block Scope:
=> Only "let/const" variables are accessed within the Block.
=> "var" can accessed outside the Block also.
=> Used especially in block "{}".

Example-1:

var a = 5;
if(a === 5){
    let b = 15;
    console.log(b);  // 15;
}
console.log(b);   // error 

Example-2:

var a = 5;
if(a === 5){
    var b = 150;
    console.log(b);  // 150
}
console.log(b);  // 150

Lexical Scope:

=> Ability for an inner scope to access the variable from outer scope 
    i.e. inner scope is lexically bound to the outer scope.
=> Lexical scope means a function can access variables from its outer scope 
    based on where the function is defined in the code, not where it is executed.

NOTE: 
JavaScript looks for variables in this order:
a) Current scope
b) Outer scope
c) Global scope

Eg: let message = "Hello";
    
    function outer(){
        let name = "Yatheswar";

        function inner(){
            console.log(message);
            console.log(name);
        }

        inner();
    }

    outer;

=====================================================================================================

Hoisting:

=> It is a JavaScript mechanism where "variables" and "function declarations" are moved to 
    top of their scope before code execution.
=> variables declared using "var" only supports hoisting.

Example-1: 

a = 10;
console.log(a);   // 10
var a;            // error

Example-2:

func();
function func(){
    console.log("Hello");
}

Example-3:

console.log(a);   // undefined 
var a = 10;

Example-4:

console.log(a);
a = 10;           // error 


=====================================================================================================

Closures:   (Function + Lexical Environment)

=> If a function is created inside another function, it retains access to scope of that outer function 
   even after that outer function returns, because the outer function is in the lexical scope of the
   function.

=> If in a inner function if the "child function is returned" then it is a CLOSURE.

Example:

function parentFunction(){

    // Lexical Scope START

    const message = "Hi!, I am parent";

    function childFunction(){
        console.log(message);
    }

    return childFunction;

    // Lexical Scope END
}

const ans = parentFunction();
console.log(ans);

The above is 16th topic, I haven't created any js file because I am not getting any idea for this. 
Also as you can see I didn't use the HTML file very much so suggest few changes we can do for HTML.
Help me in creating html, js and README files with required information and codes with nice examples for better understanding.

The above is the 15th topic, analyze the html and js files and notes.txt and do the same thing as you did for previous folders. 
Also as you can see we used HTML file so you can do changes in HTML also, if required and Help me in creating html, js and README files 
with required information and codes with nice examples for better understanding.

The above is 14th topic, I haven't created any html or js files because I am not getting any idea for this.
Help me in creating html, js and README files with required information and codes with nice examples for better understanding.

The above is the 11th topic, analyze the html and js files and do the same thing as you did for previous folders. 
Also as you can see I didn't use the HTML file very much so suggest few changes we can do for HTML.

The above is the 7th topic, I have not written any notes for this topic, so generate a new README file, analyze the html and js files and do the same thing as you did for previous folders. 
Also as you can see we used HTML file so you can do changes in HTML also, if required.